import random

# Use a fixed seed for reproducibility as requested by typical scientific standards
random.seed(2026)
selected_index = random.randint(1, 10)
print(f"Selected Idea Index: {selected_index}")
