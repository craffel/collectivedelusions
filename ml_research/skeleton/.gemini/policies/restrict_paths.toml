[[rule]]
toolName = "run_shell_command"
# Deny if the command contains parent directory traversal (..) or attempts to use absolute root paths ( /... )
argsPattern = '(\.\.| \/)'
decision = "deny"
denyMessage = "Security Policy: You are restricted to the current working directory. Do not use absolute paths or '..'."
priority = 100
