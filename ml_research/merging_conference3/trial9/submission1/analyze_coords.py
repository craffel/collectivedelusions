import torch
import numpy as np

# Load tensors from previous run or run quickly
# Since they are not saved to disk, let's write a quick analysis script that loads run_real_world.py's variables
# Actually, let's modify run_real_world.py to print some debug stats.
